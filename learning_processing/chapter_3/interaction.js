function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(0);
    
    stroke(0);
    fill(175);
    rectMode(CENTER);
                            rect(mouseX, mouseY, 50, 50); 
}
